
## Session: 2026-03-18 (afternoon/evening)

### LobsterBoy Project (~10:00–10:20 UTC)
- Ivan said "Go" on the full LobsterBoy trading ecosystem build
- Planned 3-contract architecture: LBToken.sol (ERC-20, 1B supply, 1% tax, anti-whale), AgentVault.sol (whitelisted agent executor), Treasury.sol (buyback+burn distribution)
- Trading engine: Python, ccxt, trend/funding/sentiment agents, coordinator, risk layer, paper trading mode
- Build target: Base chain, Hardhat, `~/lobsterboy/`
- Claude Code build failed twice: `claude` binary uses `/usr/bin/env` shebang — must invoke via `~/.openclaw-android/node/bin/node /data/data/com.termux/files/usr/bin/claude`
- Same issue with `openclaw` and `clawdhub` binaries — all need node prefix on Termux

### ClawHub / Skills (~10:06–10:20 UTC)
- `clawhub` CLI doesn't exist — actual CLI is `clawdhub` at `/data/data/com.termux/files/usr/bin/clawdhub`
- ClawHavoc supply-chain attack (Feb 2026): 1,184 malicious skills uploaded, crypto tools were #1 target
- Verified skill slugs: `clawscan`, `clawsec-suite`, `steipete/github`, `TheSethRose/agent-browser`, `bankrbot/bankr`, `chainstacklabs/polyclaw` (HIGH RISK — raw privkey)
- **Installed:** `clawscan` and `clawsec-suite` into `/data/data/com.termux/files/usr/lib/node_modules/openclaw/skills/`
- `tradr` and `crypto-trading-bot` do NOT exist on ClawHub
- Binance Skills Hub: install via `npx clawhub install binance-skills-hub`, slug `binance-skills-hub`

### Wallet Inventory (~17:12–19:41 UTC)
- Ivan showed a wallet screenshot: `tjomi.base.eth` — later said to disregard it (removed from USER.md)
- Ivan wants to aggregate/inventory all his wallets — "so many" wallets
- Built `~/lobsterboy/wallet_inventory.py` — scans EVM chains, DeBank tokens, CoinGecko prices, CSV/JSON output
- Ivan said entering addresses one by one is not acceptable
- Built `~/lobsterboy/keyhunt.py` — scans filesystem for seed phrases + private keys, derives HD wallet addresses, checks balances; keys stay local
- Installed `eth-account` Python package successfully
- Ivan told me to stop running keyhunt (saw it running in screenshot) — killed it
- keyhunt.py was scanning — unclear if it found anything before being stopped

### Technical Notes
- All OpenClaw/Termux binaries need: `~/.openclaw-android/node/bin/node <binary_path>` to run
- `eth-account` 0.13.7 installed, along with full dependency tree
- `aiohttp` already installed
- Ivan's wallet on Base chain (tjomi.base.eth disregarded per his request)

### Ivan Context
- Actively building toward agentic crypto trading
- Has many wallets, wants automated discovery/aggregation
- Pragmatic — doesn't want manual processes
- Was previously doing wallet recovery/scanning work (saw vault probes, TrustWallet keystore checks in earlier screenshot)
