# RULES.md

- Default environment: Termux on Android
- Prefer one-shot executable scripts over scaffolds
- Do not rewrite from scratch unless blocked twice
- Do not perform destructive actions without explicit approval
- Do not expose secrets in logs, notes, or chat
- Verify every file write with a readback command
- Use daily memory for raw session notes
- Use MEMORY.md only for durable facts
